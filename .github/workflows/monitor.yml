name: ISIMS Monitor

on:
  schedule:
    # Every 15 minutes. GitHub may delay slightly under load — that's fine
    # for this use case.
    - cron: '*/15 * * * *'
  workflow_dispatch: {} # lets you trigger a run manually from the Actions tab

jobs:
  check:
<<<<<<< HEAD
    runs-on: ubuntu-latest
=======
    # isimsf.rnu.tn is on the Tunisian university network (RNU) and is not
    # reliably reachable from GitHub-hosted runners (Azure US/EU IPs) or from
    # Render's US/EU datacenter IPs — both get network timeouts even though
    # the site loads fine from a normal Tunisian internet connection.
    # This job therefore runs on a self-hosted runner registered from a
    # machine on a Tunisian ISP/network. See README.md "Self-hosted runner
    # setup" for how to register one.
    runs-on: [self-hosted, isims-monitor]
>>>>>>> b90567d (fix(monitor): switch check job to self-hosted runner to bypass RNU network block)
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - run: npm ci

      - run: node scripts/run-monitor-once.js
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
